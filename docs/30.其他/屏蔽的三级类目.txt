        
        /** 互医租户 **/
        nethpTenant = "JD8888";

        animalTenant = "PET1000";

        /** 宠物医院 **/
        if(businessIdentity.getTenantId().equals(animalTenant)){
            /** 都是三级类目 **/
            return NewList(21070L,21069L,28320L,28321L);
        }

        /**  不是互医返回空 **/
        if(!businessIdentity.getTenantId().equals(nethpTenant)){
            return NewList();
        }

        nethpBusinessScene = 1;

        if(businessIdentity.getBusinessScene() != nethpBusinessScene){
            return NewList();
        }

        /**中医调剂师屏蔽类目**/
        chineseMedicinePharmacistHiddenCategoryList = NewList(21920L, 21922L,21921L,21925L,21923L,21924L,21915L,21919L,21918L,21917L,21916L,21910L,21914L,21911L,21913L,21912L);

        /**其他非医职业需屏蔽的类目**/
        otherNonMedicalProfessionHiddenCategoryList = new ArrayList();
        otherNonMedicalProfessionHiddenCategoryList.add(27165L);
        for (i = 0; i < chineseMedicinePharmacistHiddenCategoryList.size(); i++) {
            otherNonMedicalProfessionHiddenCategoryList.add(chineseMedicinePharmacistHiddenCategoryList.get(i));
        }

        /**中医调剂师**/
        chineseMedicinePharmacistProfession = 8;

        /**药师**/
        pharmacistProfession = 3;

        /**医生**/
        doctorProfession = 1;

        /** 当前医生职业 **/
        currentDoctorProfesstionType = doctorBaseInfo.getProfessionType();

        /** 医生和药师无屏蔽 **/
        if(currentDoctorProfesstionType == doctorProfession || currentDoctorProfesstionType == pharmacistProfession){
            return NewList();
        }

        /** 中医调剂师返回 **/
        if(currentDoctorProfesstionType == chineseMedicinePharmacistProfession){
            return chineseMedicinePharmacistHiddenCategoryList;
        }

        /** 其他非医 **/
        return otherNonMedicalProfessionHiddenCategoryList;
